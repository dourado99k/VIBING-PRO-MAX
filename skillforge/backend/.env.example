# Opção 1 — SQLite (padrão, sem Docker)
DATABASE_URL="file:./dev.db"

# Opção 2 — MySQL (altere provider em prisma/schema.prisma para "mysql")
# DATABASE_URL="mysql://skillforge:skillforge@localhost:3306/skillforge"

JWT_SECRET="skillforge-hackathon-secret-change-in-production"
JWT_EXPIRES_IN="7d"
PORT=3001
NODE_ENV=development
